{
  "name": "twinkly-edge",
  "version": "0.7.0",
  "scripts": {
    "check:lua": "((command -v lua >/dev/null 2>&1) >/dev/null 2>&1) || (echo 'Lua not installed. Please install Lua before proceeding.' && exit 1)",
    "build": "mkdir -p .stedge && smartthings edge:drivers:package --build-only .stedge/twinkly-edge.zip .",
    "debug": "smartthings edge:drivers:logcat --log-level DEBUG",
    "test": "lua run-tests.lua"
  }
}
